# NammaCraft - Heritage Art & Craft

NammaCraft is a premium platform for discovering and collecting authentic Indian heritage crafts.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Set your `GEMINI_API_KEY` in a `.env` file.
3. Run the app:
   `npm run dev`

