---
description: Start the automatic git push script
---

This workflow starts the `autopush.py` script which monitors file changes and automatically commits/pushes them to GitHub.

// turbo
1. Run the autopush script
```powershell
python autopush.py
```
